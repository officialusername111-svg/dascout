# Copy to .env.local and fill in. .env.local is git-ignored — keep it that way.
#
# Supabase dashboard -> Project Settings -> API
#   SUPABASE_URL              the project URL (safe to share)
#   SUPABASE_SERVICE_ROLE_KEY the service_role secret — FULL database access,
#                             bypasses row-level security. Never commit it,
#                             never paste it into a chat, never ship it to a browser.

SUPABASE_URL=https://kogpuuidawbmttyswvsx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
